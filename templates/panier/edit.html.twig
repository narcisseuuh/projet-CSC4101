{% extends 'base.html.twig' %}

{% block title %}Editer le Panier{% endblock %}

{% block body %}
    <h1>Editer le Panier</h1>

    {{ include('panier/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-primary" href="{{ path('app_member_show', {'id' : panier.getCreator.getId }) }}">retour à la liste des paniers</a>

    {{ include('panier/_delete_form.html.twig') }}
{% endblock %}
